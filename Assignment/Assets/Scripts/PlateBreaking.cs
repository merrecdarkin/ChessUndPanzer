using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlateBreaking : MonoBehaviour
{
    // Start is called before the first frame update
    public void BreakDown() {
            Destroy(gameObject);
    }
}
