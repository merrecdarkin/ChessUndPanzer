using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MidModeBeat : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {StaticTracker.E2beaten=true;
        
    }

    // Update is called once per frame
    
}
