using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EzModeBeat : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {StaticTracker.E1beaten=true;
        
    }

    // Update is called once per frame
    
}
