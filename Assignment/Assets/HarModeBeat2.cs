using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HarModeBeat : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {StaticTracker.E3beaten=true;
        
    }

    // Update is called once per frame
    
}
